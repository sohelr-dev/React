export interface duration {
    id?: number,
    description?: string
    name?: string
}

const durationDefault: duration = {
    id: 0,
    description:"",
    name:""
};

export default durationDefault;